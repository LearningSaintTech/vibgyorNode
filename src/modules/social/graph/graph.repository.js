const User = require('../../user/user.model');
const Report = require('./userReport.model');
const FollowRequest = require('./followRequest.model');
const Post = require('../post/post.model');

module.exports = {
	User,
	Report,
	FollowRequest,
	Post,
};
