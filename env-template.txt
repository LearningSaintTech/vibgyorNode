# Backend Environment Variables Template
# Copy this content to vibgyor-backend/.env and update with your values

# Environment
NODE_ENV=development

# Server Configuration
PORT=3000
SHUTDOWN_TIMEOUT=10000
APP_URL=http://localhost:3000

# Render.com Environment Variables (set these in Render dashboard)
# NODE_ENV=production
# PORT=10000 (automatically provided by Render)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dbname
# JWT_SECRET=your_jwt_secret_key
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password

# Frontend Environment Variables Template
# Copy this content to vibgyor-frontend/.env and update with your local IP address

# API Configuration
VITE_API_URL=https://YOUR_LOCAL_IP:3000
VITE_SOCKET_URL=https://YOUR_LOCAL_IP:3000

# Environment
VITE_ENVIRONMENT=development

# Instructions:
# 1. Replace YOUR_LOCAL_IP with your actual local IP address
# 2. Find your IP using: ipconfig (Windows) or ifconfig (Mac/Linux)
# 3. Make sure both devices are on the same WiFi network
# 4. Ensure HTTPS certificates are set up (see setup-https.md)

